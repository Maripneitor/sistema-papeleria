{
  "name": "sistema-papeleria-monorepo",
  "version": "1.0.0",
  "description": "Sistema integral de gestión para papelería (Full Stack)",
  "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"bgBlue.bold,bgGreen.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && npx ts-node src/seed.ts"
  },
  "keywords": [],
  "author": "Maripneitor",
  "license": "ISC",
  "devDependencies": {
    "@types/styled-components": "^5.1.36",
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "styled-components": "^6.1.19"
  }
}
